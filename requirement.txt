Flask
Flask-JWT-Extended
response
Flask-SQLAlchemy
validators
marshmallow
flasgger
flask-marshmallow

"""starter = file.find(',')
    image_data = file[starter + 1:]
    image_data = bytes(image_data, encoding="ascii")
    img = Image.open(BytesIO(base64.b64decode(image_data)))
    img.save('./src/doc/image.jpg')

    return jsonify({
        'msg': 'success',
        'img': os.path.abspath("./src/doc/image.jpg")
    })"""