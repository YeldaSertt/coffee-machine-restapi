Coffee-Make
---
tags:
  - COFFEEMAKE
parameters :
  - name: body
    description: Coffee-Make Machine
    in: body
    required : true
    schema:
      type: object
      required:
        - "drink_name"
        - "water"
        - "milk"
        - "coffee"
      properties:
        drink_name:
          type: "string"
          example: "latte"
        water:
          type: "integer"
          example: "30"
        milk:
          type: "integer"
          example: "30"
        coffee:
          type: "integer"
          example: "30"


responses:
  200:
    description: When a user successfully logs in

  400:
    description: Fails to login due to bad request data

  401:
    description: A user supplies incorrect credentials
