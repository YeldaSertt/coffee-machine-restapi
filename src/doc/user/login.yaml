user log in
---
tags:
  - Authentication
parameters :
  - name: body
    description : Sadece Admin girisidir!!!
    in :  body
    required : true
    schema:
      type: object
      required:
        - "name"
        - "password"
      properties:
        name:
          type: "string"
          example: "user"
        password:
          type: "string"
          format: password
          example: "************"
responses:
  200:
    description: When a user successfully logs in

  400:
    description: Fails to login due to bad request data

  401:
    description: A user supplies incorrect credentials